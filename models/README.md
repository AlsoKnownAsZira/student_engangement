# Place your model weights here:
#   - yolo11s.pt        (person detection model — copy from project root)
#   - best.pt           (trained engagement classifier — from runs/classify/weights/)
#
# These files are NOT committed to git (see .gitignore).
